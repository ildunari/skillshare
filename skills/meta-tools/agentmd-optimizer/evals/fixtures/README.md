# Eval Fixtures

Populate this directory with small synthetic trees that cover:
- exact duplicate clusters
- near-duplicate variants
- semantic overlap without deletion safety
- deleted-tree / stale-audit cases
- active vs archive vs mirror layouts
- skill install topology across runtimes
- conflicting instruction chains
