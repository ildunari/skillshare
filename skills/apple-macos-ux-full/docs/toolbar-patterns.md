# Toolbar Patterns

- Prefer **unified toolbars** to merge title and toolbar for modern appearance.
- Expose **Customize Toolbar…** when customization adds value.
- Keep toolbars **sparse and meaningful**; avoid checkbox clutter.
- Center important composite controls with `window.toolbar?.centeredItemIdentifier`.
