# Migration Strategies

1. Identify complex legacy screens → bridge them.
2. Embed SwiftUI islands via hosting controllers.
3. Move logic into shared models; keep UIKit/AppKit as renderers.
4. Replace leaf screens with pure SwiftUI as confidence grows.
