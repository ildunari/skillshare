# SwiftUI Performance

- Understand identity vs. state; use the SwiftUI instrument for visibility. citeturn8search3
- Use `.equatable()` / `EquatableView` to skip updates when inputs didn't change. citeturn17search4
- Prefer `LazyVStack/LazyHGrid` for long lists. citeturn17search1
- Avoid unnecessary `.id(_:)` which resets view identity. citeturn8search10
