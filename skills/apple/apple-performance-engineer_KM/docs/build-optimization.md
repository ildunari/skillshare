# Build Optimization

- Improve incremental builds by simplifying dependencies and breaking up monolithic targets. citeturn10search0
- Use the Build Timing Summary to find slow files and phases; add type hints where needed. citeturn10search4
- Prefer Incremental (Debug) and WMO (Release). citeturn10search16
- Declare outputs of Run Script phases to prevent unnecessary rebuilds. citeturn10search17
