
# SwiftUI Review Checklist

- [ ] Views are small and composable
- [ ] Heavy work off main thread; use `@MainActor` for UI updates
- [ ] `@State`, `@Binding`, `@ObservedObject`, `@EnvironmentObject` used correctly
- [ ] Accessibility: labels, hints, traits, Dynamic Type
- [ ] Preview providers compile and represent edge cases
