# Machine-specific config (has local target paths)
# Canonical Hermes allowlist is tracked in hermes-allowlist.yaml and applied via
# scripts/sync_hermes_targets.py. Do not treat config.yaml as the source of truth.
config.yaml

# macOS
.DS_Store

# Python cache
__pycache__/
*.pyc

# Temp/cache
*.tmp
*.bak
*.swp

# Private skills — skillshare-managed locally, never pushed to public repo.
skills/documents-office/fixing-office-accessibility/
skills/documents-office/using-a11yfix/
